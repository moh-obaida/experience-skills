# Authored Design-System Examples

These are invented, deterministic reference implementations. They are not real-product precedent or copyable production templates. `tests/fixtures/pages/signal-foundry.html` demonstrates the Signal Foundry system's status hierarchy, compact queue, token roles, keyboard focus, and desktop-to-mobile layout transformation. Replace its content, semantics, and breakpoints with those justified by the actual product; verify sparse, dense, error, and zoom states before reuse.
